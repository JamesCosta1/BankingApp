namespace Models
{
    // Defines a mini dictionary-thingy that assigns a word to a value. I do not remember why I needed this...
    public enum AccountType
    {
        undefined,
        checking,
        savings
    }
}